<!DOCTYPE html>
<html>
    <head>
        <title>More involved</title>
    </head>
    <body>
        <h1>More involved</h1>
        {{~#if name~}}
            <p>Hi, {{name}}</p>
        {{~else~}}
            <p>Hi, stranger</p>
        {{~/if~}}
        {{~#if age~}}
            <p>Aged {{age}}</p>
        {{~else~}}
            <p>you baby</p>
        {{~/if~}}
    </body>
</html>